
  - separate import lines
- integrate husky
  - https://github.com/typicode/husky
- update scaffolding script to:
  - Include function arguments in params
  - Super nice to have: append to existing files rather than erroring

- Tiers
  -  add exported function for tier (default 1) with default arguments
  - import into index.ts
  - object.values
  - add to tracking.csv

- Make a video for the contribution section
- Finalize the contribution section
- Create script to clean up the code copy-pasted from 
- Create script for adding a tier to an existing product
  1. Determine if a bank account is being tracked in tracking.csv
  2. Fork the repo
  3. Clone
  4. 
- Add screenshots for the getting started section
- Implement unit testing framework (Jest)